#!/bin/bash
set -euo pipefail

cd "$(dirname "$0")/.."
mkdir -p target/enums

for e in action_state attack character item stage; do
	clj -M -m peppi-codegen.enums "resources/enums/$e.json" | rustfmt > "target/enums/$e.rs"
done
